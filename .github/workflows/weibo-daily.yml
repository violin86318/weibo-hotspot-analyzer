name: 微博热搜每日分析

on:
  schedule:
    # 每天 UTC 02:00 执行 (北京时间 10:00)
    - cron: '0 2 * * *'
    # 每天 UTC 14:00 执行 (北京时间 22:00)
    - cron: '0 14 * * *'
  workflow_dispatch: # 支持手动触发

permissions:
  contents: write
  pages: write
  id-token: write

# 并发控制
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  weibo-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip

      - name: 抓取微博热搜
        env:
          TIANAPI_KEY: ${{ secrets.TIANAPI_KEY }}
        run: |
          python scripts/fetch_weibo_hot.py

      - name: Claude AI 分析创意
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }}
          API_KEY: ${{ secrets.API_KEY }}
          API_MODEL: ${{ secrets.API_MODEL }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python scripts/claude_analysis.py

      - name: 生成 HTML 报告
        run: |
          python scripts/generate_html_report.py

      - name: 创建输出目录
        run: |
          mkdir -p reports/$(date +%Y/%m)

      - name: 移动报告到输出目录
        run: |
          REPORT_FILE=$(ls -t weibo_hotspot_report_*.html 2>/dev/null | head -1)
          if [ -n "$REPORT_FILE" ]; then
            # 使用时间戳避免覆盖：2026-01-18_220000
            TIMESTAMP=$(date +%Y-%m-%d_%H%M%S)
            cp "$REPORT_FILE" "reports/$(date +%Y/%m)/${TIMESTAMP}_weibo_hotspot_report.html"
            echo "✅ 报告已复制到: reports/$(date +%Y/%m)/${TIMESTAMP}_weibo_hotspot_report.html"

            # 同时创建一个 latest 副本（方便访问最新报告）
            cp "$REPORT_FILE" "reports/$(date +%Y/%m)/latest_weibo_hotspot_report.html"
          else
            echo "❌ 未找到生成的报告文件"
            exit 1
          fi

      - name: 提交报告
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add reports/
          git diff --staged --quiet || git commit -m "🤖 自动生成微博热搜报告 $(date +%Y-%m-%d)"
          git push

      # 部署到 GitHub Pages
      - name: 准备 Pages 部署
        run: |
          mkdir -p pages

          # 优先使用 latest 副本作为首页（如果有）
          LATEST_REPORT=$(find reports -name "latest_weibo_hotspot_report.html" -type f | sort -r | head -1)

          # 如果没有 latest，使用最新的时间戳报告
          if [ -z "$LATEST_REPORT" ]; then
            LATEST_REPORT=$(find reports -name "*_weibo_hotspot_report.html" -type f ! -name "latest_*" | sort -r | head -1)
          fi

          if [ -n "$LATEST_REPORT" ]; then
            cp "$LATEST_REPORT" pages/index.html
            echo "✅ 首页更新为: $LATEST_REPORT"
          fi

          # 复制所有历史报告
          cp -r reports/* pages/ 2>/dev/null || true

          # 生成历史报告列表页面（使用 Python 脚本避免 YAML heredoc 语法错误）
          echo "📋 生成历史报告列表页面..."
          python scripts/generate_reports_list.py

      - name: 上传到 GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: weibo-analysis
    
    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
